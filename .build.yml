image: alpine/edge
oauth: pages.sr.ht/PAGES:RW
packages:
- hut
environment:
  site: raiguard.me
tasks:
- generate: |
    cd $site
    ./build.sh
- package: |
    cd $site/static
    tar -cvz . > ../../site.tar.gz
- upload: |
    hut pages publish -d $site site.tar.gz
